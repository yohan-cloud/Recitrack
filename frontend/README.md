# ReciTrack Frontend

React + TypeScript + Vite frontend for ReciTrack.

Run from this folder:

```bash
npm install
npm run dev -- --host 127.0.0.1
```

The main setup instructions are in the root `README.md`.
